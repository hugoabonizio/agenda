<%= form_for(@copypaste) do |f| %>
  <% if @copypaste.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@copypaste.errors.count, "error") %> prohibited this copypaste from being saved:</h2>

      <ul>
      <% @copypaste.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
