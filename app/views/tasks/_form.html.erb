<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :context %><br>
    <!--<select name="task[context]" id="context_selector">
      <option value="formais">Formais</option>
      <option value="bd">B.D.</option>
      <option value="redes">Redes</option>
      <option value="so">S.O.</option>
      <option value="compiladores">Compiladores</option>
      <option value="teoria">Teoria</option>
      <option value="engenharia">Engenharia</option>
      <option value="">Outro</option>
    </select>-->
    <%= f.text_field :context, id: 'context' %>
  </div>
  <div class="field">
    <%= f.label :pririty %><br>
    <input type="range" name="task[pririty]" min="1" max="100">
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
